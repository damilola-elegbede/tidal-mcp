# Tidal MCP Server Configuration Example
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# REQUIRED: Tidal API Credentials
# Register your application at https://developer.tidal.com/
# to obtain your client ID and secret
TIDAL_CLIENT_ID=your_tidal_client_id_here
TIDAL_CLIENT_SECRET=your_tidal_client_secret_here

# OPTIONAL: OAuth Configuration
# Custom redirect URI for OAuth callback (default: http://localhost:8080/callback)
# TIDAL_REDIRECT_URI=http://localhost:8080/callback

# OPTIONAL: Regional Settings
# ISO country code for content availability (default: US)
# Supported: US, GB, DE, FR, NO, SE, DK, NL, BE, CH, AT, IT, ES, PT, PL, etc.
# TIDAL_COUNTRY_CODE=US

# OPTIONAL: Session Management
# Custom path for storing authentication session (default: ~/.tidal-mcp/session.json)
# TIDAL_SESSION_PATH=~/.tidal-mcp/session.json

# OPTIONAL: Audio Quality
# Streaming quality preference: LOW, HIGH, LOSSLESS, HI_RES
# TIDAL_QUALITY=HIGH

# OPTIONAL: Development Settings
# Enable debug logging (default: false)
# TIDAL_DEBUG=false

# OPTIONAL: API Configuration
# Custom API base URL (for testing or enterprise environments)
# TIDAL_API_BASE_URL=https://api.tidal.com

# OPTIONAL: Rate Limiting
# Max requests per second (default: 10)
# TIDAL_RATE_LIMIT=10